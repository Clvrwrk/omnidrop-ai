-- =============================================================================
-- OmniDrop AI — Development Seed Data
-- Applied to local Supabase instance only (never to staging/production)
-- Run with: supabase db seed
-- =============================================================================

-- TODO: Add seed data once tables are created in migrations
-- Example:
-- INSERT INTO jobs (acculynx_job_id, status) VALUES ('test-job-001', 'pending');
